import React from 'react'

const GlobalFeed = () => {
  const data =  [
        {
            "id":2,
            "title":"Folder 1",
            "parentId":-1
        },
        {
            "id":3,
            "title":"Test",
            "parentId":1
        },
        {
            "id":-1,
            "title":"Folder 2",
            "parentId":null
        },
        {
            "id":1,
            "title":"Folder 2",
            "parentId":-1
        },
        {
            "id":4,
            "title":"Wrike",
            "parentId":2
        },
        {
            "id":5,
            "title":"Tester",
            "parentId":1
        }
    ]

    // const toTree = (arr, idAttr = 'id', parentAttr = 'parentId') => {
    //     arr.sort((a, b) =>
    //         (a[parentAttr] === b[idAttr] && 1) ||
    //         (b[parentAttr] === a[idAttr] && -1) ||
    //         0)
    //     const tree = arr.reduce((tree, data) => {
    //         data.children = []
    //         tree[data[idAttr]] = data;
    //         (tree[data[parentAttr]] || tree).children.push(data);
    //         return tree
    //     }, { children: [] }).children;
    //     return tree
    // }

    // const createTree = (data, idField, parentField, rootParent) => {
    //     const tree = { [rootParent]: { children: [] } };
    //     data.forEach(n => tree[n[idField]] = { ...n, children: [] });
    //     data.forEach(n => tree[n[parentField]].children.push(tree[n[idField]]));
    //     return tree[rootParent].children;
    // };

    // const tree = createTree(data, 'id', 'parentId', null);
    // console.log(tree)

    return <div>GlobalFeed</div>
}

export default GlobalFeed
